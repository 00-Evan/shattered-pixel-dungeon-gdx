diff a/core/src/com/watabou/pixeldungeon/sprites/ItemSprite.java b/core/src/com/watabou/pixeldungeon/sprites/ItemSprite.java	(rejected hunks)
@@ -140,9 +140,9 @@ public class ItemSprite extends MovieClip {
 			place( from );
 	
 			speed.offset( (px-x) / DROP_INTERVAL, (py-y) / DROP_INTERVAL );
-			
-			Log.d( "GAME", toString() );
-			Log.d( "GAME", String.format( "drop aside: %.1f %.1f", speed.x, speed.y ) );
+
+            Log.d( "GAME", toString() );
+            Log.d( "GAME", String.format( "drop aside: %.1f %.1f", speed.x, speed.y ) );
 		}
 	}
 	
