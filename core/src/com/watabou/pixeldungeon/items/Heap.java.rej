diff a/core/src/com/watabou/pixeldungeon/items/Heap.java b/core/src/com/watabou/pixeldungeon/items/Heap.java	(rejected hunks)
@@ -18,8 +18,10 @@
 package com.watabou.pixeldungeon.items;
 
 import java.util.Collection;
+import java.util.Collections;
 import java.util.LinkedList;
 
+import com.watabou.pixeldungeon.items.food.Blandfruit;
 import com.watabou.noosa.audio.Sample;
 import com.watabou.pixeldungeon.Assets;
 import com.watabou.pixeldungeon.Badges;
@@ -325,7 +341,8 @@ public class Heap implements Bundlable {
 	public void restoreFromBundle( Bundle bundle ) {
 		pos = bundle.getInt( POS );
 		type = Type.valueOf( bundle.getString( TYPE ) );
-		items = new LinkedList<Item>( (Collection<? extends Item>) bundle.getCollection( ITEMS ) ); 
+		items = new LinkedList<Item>( (Collection<? extends Item>) bundle.getCollection( ITEMS ) );
+        items.removeAll(Collections.singleton(null));
 	}
 
 	@Override
