diff a/core/src/com/watabou/pixeldungeon/levels/Level.java b/core/src/com/watabou/pixeldungeon/levels/Level.java	(rejected hunks)
@@ -282,10 +326,12 @@ public abstract class Level implements Bundlable {
 		bundle.put( MAPPED, mapped );
 		bundle.put( ENTRANCE, entrance );
 		bundle.put( EXIT, exit );
+        bundle.put( LOCKED, locked );
 		bundle.put( HEAPS, heaps.values() );
 		bundle.put( PLANTS, plants.values() );
 		bundle.put( MOBS, mobs );
 		bundle.put( BLOBS, blobs.values() );
+        bundle.put( FALLING, fallingItems);
 	}
 	
 	public int tunnelTile() {
